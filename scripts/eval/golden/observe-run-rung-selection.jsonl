{"id":"rung1-single-span-count-inprocess","input":"Claim: the order-checkout handler emits exactly one span for a single order. The claim is checked entirely within the same process, no other services are called, and the developer wants an instant offline read with no CLI dependency.","expected":"rung-1","notes":"Rung 1 table: Cost instant/no network hop; Availability offline, no Dash0 CLI; single-process claim needs no rung-2 CLI dependency."}
{"id":"rung1-health-endpoint-single-process","input":"Verify that hitting a Node service's /health endpoint creates exactly one span in that same service process; nothing else is involved.","expected":"rung-1","notes":"Rung 1 Coverage row: only the process(es) explicitly wired to the in-memory/file exporter; single-process claim needs no cross-process visibility."}
{"id":"rung1-span-status-error-local","input":"Confirm a function's span status flips to Error when the wrapped call throws, observed locally via OTEL_TRACES_EXPORTER=console with no network hop.","expected":"rung-1","notes":"Rung 1 Correct example names OTEL_TRACES_EXPORTER=console as the rung-1 read path; no cross-process or baseline element present."}
{"id":"rung1-retry-loop-span-count-unit-test","input":"A unit test runs a retry loop in-process and needs to assert the span count for the retried operation equals 3; no Dash0 CLI is installed on this machine.","expected":"rung-1","notes":"Rung 1 Availability row: works with no Dash0 CLI, no credentials, no network \u2014 this is the deciding property here."}
{"id":"rung1-parent-child-same-process","input":"Assert the parent/child relationship between two spans created within the same request handler, both produced by the one process under test.","expected":"rung-1","notes":"Both spans originate in one process; rung 1's Coverage row (only the process wired to the exporter) already decides this claim."}
{"id":"rung1-offline-no-credentials","input":"The developer has no Dash0 credentials and no network access; they want to see, instantly and in full, the spans a single CLI script just emitted.","expected":"rung-1","notes":"Rung 1 Span content row: available immediately, in full \u2014 matches the no-credentials, no-network constraint exactly."}
{"id":"rung1-duration-bound-inprocess","input":"Claim: a batch-job function's span duration stays under 200ms. The batch job runs entirely in one process with no calls to other services.","expected":"rung-1","notes":"Single-process duration claim; rung 1's instant, in-full span content is sufficient, no cross-process or baseline element."}
{"id":"rung1-attribute-read-local-test-harness","input":"Using the project's existing in-memory span processor test harness, read the attributes recorded on the span for one in-process function call.","expected":"rung-1","notes":"Rung 1 Correct example: a test-harness in-memory processor already wired by the project is the named rung-1 read path."}
{"id":"rung2-cross-process-fanout-payment","input":"Verify that calling the checkout API results in exactly 2 calls to the separately-deployed payment-provider microservice.","expected":"rung-2","notes":"Rung 1's own Incorrect example calls this exact shape WRONG for rung 1: a cross-process call needs rung 2."}
{"id":"rung2-baseline-comparison-prior-run","input":"Compare this run's span count for the ingest pipeline against last week's baseline run, using the same identity keys, to confirm no fan-out regression.","expected":"rung-2","notes":"Rung 2's Baseline comparability row is the only rung that stamps identity keys enabling a query against a prior run; rung 1's table has no such row."}
{"id":"rung2-downstream-service-parent-linkage","input":"The app under test calls a separately-deployed downstream service over HTTP; confirm the downstream service's own emitted span shows the correct parent linkage back to the caller.","expected":"rung-2","notes":"Cross-process claim across two deployed services; rung 1 Cross-process fan-out row states this is 'Not visible' at rung 1."}
{"id":"rung2-dash0-spans-query-content","input":"The assertion needs actual span content confirmed via dash0 spans query, filtered on the run's dev.run.id resource attribute.","expected":"rung-2","notes":"dash0 spans query is named only under Rung 2's Correct example and its Span content row ('content comes from dash0 spans query')."}
{"id":"rung2-multi-service-claim","input":"The claim spans three independently-deployed services in one request; the app under test needs zero exporter-config change since it already points at the OTLP default ports.","expected":"rung-2","notes":"Rung 2's own description: the app needs zero exporter-config change because it already points at OTel default ports, and the claim spans multiple services."}
{"id":"rung2-cross-process-fanout-gateway","input":"Need to verify cross-process fan-out from the API gateway to three backend services during a single incoming request.","expected":"rung-2","notes":"Rung 1 Cross-process fan-out row: 'Not visible' \u2014 a claim about fan-out across services is explicitly rung 2's Coverage row."}
{"id":"decoy-rung2-process-boundary-fulfilment","input":"Verify the checkout request fans out across process boundaries to the separately-deployed fulfilment worker, and that the worker's own process emits a span carrying the inbound trace context.","expected":"rung-2","notes":"DECOY (says 'process' repeatedly but is cross-process). The word alone points at rung-1; the claim spans two deployed processes, so only the proxy can observe both."}
{"id":"decoy-rung2-queue-handoff-consumer-process","input":"The claim concerns a cross-process hand-off: the API writes to a queue and a separate consumer process emits its own span. Confirm exactly one consumer span per enqueued job.","expected":"rung-2","notes":"DECOY (says 'process' twice, still rung-2). Producer and consumer are different processes, so an in-memory exporter in either sees only half the claim."}
{"id":"decoy-rung2-baseline-no-process-word","input":"Compare the current build's span count for the export job against the recorded baseline from the previous release, using the same run-identity keys.","expected":"rung-2","notes":"DECOY (no 'process' word at all). Rung-2 via the baseline-comparison trigger rather than cross-process: a prior run's spans are not available to an in-memory exporter."}
{"id":"decoy-rung1-duration-no-process-word","input":"Claim: a pure formatting helper wrapped in a span records a duration under 5ms. The whole assertion is satisfiable inside one Node test file with an in-memory exporter, offline, with no network calls.","expected":"rung-1","notes":"DECOY (never says 'process'). Single-process by description rather than by the word; rung 1 decides it instantly and offline."}
{"id":"decoy-rung1-error-status-no-process-word","input":"Assert that a validation failure sets the span status to Error. The code under test is a single library function with no network calls, and the developer wants the span content instantly and offline.","expected":"rung-1","notes":"DECOY (never says 'process'). One library function, offline, span content needed directly — the rung-1 case exactly."}
{"id":"decoy-rung1-child-span-count-no-process-word","input":"Using the existing test harness's span recorder, check that one call to the pricing function produces exactly two child spans; everything happens inside the test.","expected":"rung-1","notes":"DECOY (never says 'process'). An existing in-memory recorder already sees the whole claim."}
{"id":"decoy-rung1-caller-side-span-only","input":"The service calls a separately-deployed payment provider over HTTP, but the claim is only about the caller's own client span: confirm it records an Error status when the provider returns 500. The provider's own spans are out of scope.","expected":"rung-1","notes":"DECOY (names a separately-deployed service, still rung-1). The span under assertion is created by the process under test, so rung 1's Coverage row already sees it; rung 2 is for claims about the DOWNSTREAM service's own spans."}
{"id":"decoy-rung1-threshold-not-prior-run","input":"Compare the report-render span's duration against the 200ms baseline documented in the team's SLO — a fixed number, not a previous run's telemetry. The whole render happens inside one Node test file.","expected":"rung-1","notes":"DECOY (says 'baseline', still rung-1). Rung 2's Baseline comparability row is about querying a PRIOR RUN's stored spans; comparing against a constant needs no stored run, so rung 1 decides it offline."}
{"id":"decoy-rung1-cross-process-out-of-scope","input":"The worker also publishes to a separately-deployed notifier, but the assertion is only that the worker's own handler creates exactly two child spans per job, read with the project's in-memory recorder.","expected":"rung-1","notes":"DECOY (names a separately-deployed service, still rung-1). The cross-process hop is explicitly out of scope; every span the claim covers is emitted by the one process already wired to the exporter."}
{"id":"decoy-rung1-cross-process-word-narrow-claim","input":"The team calls this a cross-process pipeline, but the assertion under test is narrower: that the ingest function's own span carries a non-zero batch.size attribute, checked in one process with a file exporter.","expected":"rung-1","notes":"DECOY (says 'cross-process', still rung-1). The pipeline's shape is not the claim's scope — rung selection follows what the ASSERTION must observe, and this one observes a single process's own span."}
{"id":"decoy-rung2-three-services-no-keyword","input":"One request travels through the gateway, the auth sidecar, and the pricing service, each deployed on its own. Confirm the emitted trace links all three into a single connected tree.","expected":"rung-2","notes":"DECOY (rung-2 without any of the usual vocabulary). Three independently-deployed services means an in-memory exporter in any one of them sees a third of the claim; only the proxy's default-port coverage sees the whole trace."}
{"id":"decoy-rung2-prior-release-volume","input":"Confirm the new batching change did not increase span volume relative to the run recorded before the release, matched on the shared run-identity keys.","expected":"rung-2","notes":"DECOY (rung-2 without any of the usual vocabulary). A comparison against an already-recorded run needs stored, identity-keyed spans — the property rung 2's Baseline comparability row owns and rung 1's table has no row for."}
{"id":"decoy-rung2-browser-to-backend-linkage","input":"A page load in the browser and the backend handler that serves it each emit their own spans. Confirm the backend span is a child of the browser's document-load span.","expected":"rung-2","notes":"DECOY (rung-2 without any of the usual vocabulary). Two runtimes, neither able to see the other's exporter; the linkage claim needs a reader that receives both."}
{"id":"decoy-rung2-sidecar-collector-hop","input":"The app ships spans to a sidecar that enriches them before forwarding. Confirm the enrichment attribute is present on the spans that actually leave the sidecar, not merely on the ones the app handed over.","expected":"rung-2","notes":"DECOY (rung-2 without any of the usual vocabulary). An in-memory exporter inside the app sees the pre-enrichment spans only; what left the sidecar is observable solely at the receiving end."}
