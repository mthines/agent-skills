# Handoff: <task title>

> Generated <YYYY-MM-DD> from a Claude Code session on branch `<branch>` in `<repo path>`.
> Paste this into a fresh agent or LLM to continue the work. The reader has no
> prior context — everything needed is below.

## Goal

<One or two sentences: the objective and what "done" means. State the
acceptance criteria explicitly.>

## Current state

<What is finished and verified, what is in progress, what is blocked. Mark each
claim as (verified) or (assumed). Example:>

- (verified) Parser handles the `--json` flag; `pnpm test` green.
- (in progress) `--brief` flag half-wired in `src/cli.ts`.
- (blocked) Needs a decision on default output width — see Decisions below.

## Plan / next steps

<Ordered, actionable remaining steps. Each one something the reader can start
without asking a question.>

1. <step>
2. <step>

## Key files

<Every file the work touches, repo-relative, with its role / what changed.>

- `path/to/file` — <role or change>
- `path/to/other` — <role or change>

## Decisions & gotchas

<Decisions and their rationale (name rejected alternatives), plus constraints,
non-obvious dependencies, and traps to avoid.>

- **<decision>** — <why>. Rejected: <alternative> because <reason>.
- **Gotcha:** <non-obvious constraint or trap>.

## How to run / verify

<The exact commands to build, test, and run. No secrets.>

```bash
<commands>
```
